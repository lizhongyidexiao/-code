#include<stdio.h>
int main()
{
	//int arr[10]={0};
	//arr 数组名为数组----首元素的地址
	//&arr[0]----首元素的地址
	//&arr-------数组的地址 
	
	int arr[5]={1,2,3,4,5};
	int(*p)[5]=&arr;//不能写成int* p[5],因为[]优先级高，p就变成数组不能变为指针 
	
	//------下面哪个是数组指针
	//int*p1[10]是个数组，存放int*指针的数组 
	//int(*p2)[10] 是个指针，存放数组的指针p2 先和*结合 
	return 0;
}
